import styles from "./pageplug.module.scss"

const Pageplug = () => {
	return <section className={styles.pageplug}>
		<div className={styles.pageplug__wrapper}>
		</div>
	</section>
}

export default Pageplug